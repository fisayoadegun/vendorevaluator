@model GMTVendorEvaluationWebApp.Models.Criteria

    <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
        @{
            ViewData["Title"] = "Details";
        }

        <h1>Criteria Details</h1>

        <div>
            <h4>Criteria</h4>
            <hr />
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.criteria_name)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.criteria_name)
                </dd>
            </dl>
        </div>
        <div>
            <a asp-action="Edit" asp-route-id="@Model.criteriaID">Edit</a> |
            <a asp-action="Index">Back to List</a>
        </div>
</div>